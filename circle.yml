machine:
  timezone:
    Japan
  ruby:
    version: 2.2.3

dependencies:
  post:
    - npm run build 

deployment:
  production:
    branch: release
    commands:
      - bundle exec cap production deploy
